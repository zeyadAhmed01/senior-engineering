---
type: llm
---
PASS if the response keeps the outcome as a recommendation, requires mapping current boundaries and pain points, compares credible alternatives and migration costs, and gives evidence-based decision triggers without editing code.

FAIL if it assumes microservices are inherently better, claims a repository-specific recommendation without evidence, edits code, or expands into release execution.

